Set up my SuperClaude Claude Code toolkit on this machine.

1. Add the plugin marketplace: justinw916-sketch/SuperClaudePublic
   (in the Desktop app: + -> Plugins -> Add plugin -> add that repo;
    or CLI: claude plugin marketplace add justinw916-sketch/SuperClaudePublic)
2. Install the plugin named "claude-code-toolkit" from that marketplace.
3. (Optional) The core operating rules already load automatically via the plugin. Only to personalize identity, copy reference/CLAUDE.md into my
   ~/.claude/CLAUDE.md. If ~/.claude/CLAUDE.md already exists, back it up first
   (append a timestamp) before writing.
4. If ~/.claude/commands/ already contains plan.md, stage.md, handoff.md,
   verify.md, reset.md, ship.md, or trim.md, move them (and any old
   ~/.claude/skills/bounded-build) into ~/.claude/_pre-plugin-backup/ so the
   plugin is the single source of those commands.
5. Verify the install: confirm /plan, /stage, /handoff, /verify, /review, /ship
   are available and that the bounded-build, operating-rules, and review skills
   load. Report exactly what was installed, moved, and verified.

Do not touch any other files in ~/.claude. Do not commit or push anything.
